document.addEventListener('DOMContentLoaded', function() { makeGroupNameLinksClickable(); setInterval(makeGroupNameLinksClickable, 1000); });
function makeGroupNameLinksClickable() { const senderElements = document.querySelectorAll('.message-sender'); senderElements.forEach(function(element) { if (element.querySelector('a')) return; const username = element.textContent.trim(); const linkElement = document.createElement('a'); linkElement.href = '/profile/' + username; linkElement.className = 'text-primary text-decoration-none'; linkElement.textContent = username; element.innerHTML = ''; element.appendChild(linkElement); }); }
